# Example environment variables for CampusNet backend
# Copy this to a file named .env and fill in real values (do NOT commit .env)

MONGO_URI=mongodb://localhost:27017/campusnet
JWT_SECRET=replace_with_a_long_random_secret
PORT=5000
NODE_ENV=development
# Optional: comma-separated list of allowed CORS origins (frontend dev hosts, production origin, etc.)
# CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
